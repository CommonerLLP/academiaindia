# India HEI Job Tracker — runtime dependencies
#
# Versions pinned to the set verified against this codebase. Bumping any of
# these is a deliberate change — Pydantic 3.x will break our `model_dump()`
# usage, requests-cache has had several BC breaks in minor releases, and
# bs4's HTML-parser semantics shift quietly.
#
# To upgrade in a controlled way:
#   .venv/bin/pip install -U <package>
#   make test                # runs the unit tests
#   make scrape --limit 5    # smoke-test 5 institutions
# Then commit the new pin with a one-line note.

requests==2.33.1
requests-cache==1.3.1
pydantic==2.13.3
beautifulsoup4==4.14.3
lxml==6.1.0

# Parliamentary-record corpus refresh — fetched directly from the public
# CommonerLLP repo at a pinned tag rather than PyPI (no PyPI release yet
# for this package). Use the `[http,pdf]` extras: the host project already
# pins `requests`, but the package's clean-install path expects to discover
# it via the extra; `pdfminer.six` is the fallback PDF text-extractor used
# when `pdftotext` (system binary) isn't available. Bump the @v tag when a
# new release lands at github.com/CommonerLLP/sansad-semantic-crawler.
sansad-semantic-crawler[http,pdf] @ git+https://github.com/CommonerLLP/sansad-semantic-crawler.git@v0.3.0
